{
  "name": "aiqbrain-landing",
  "version": "1.0.0",
  "description": "AIQBrain cloaking worker",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "test": "node --test scripts/__tests__/validate_offers.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
