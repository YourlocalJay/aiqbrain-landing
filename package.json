{
  "name": "aiqbrain-landing",
  "version": "1.0.0",
  "description": "AIQBrain landing page using Cloudflare Workers",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "dependencies": {
    "itty-router": "^3.0.12"
  },
  "devDependencies": {
    "wrangler": "^3.10.0"
  }
}